## Summary

What this PR does and why, in a sentence or two.

## Changes

-
-

## Testing done

- [ ] `make verify` passes locally (lint + typecheck + tests) — paste the
      tail of its output below
- [ ] New/changed behavior is covered by tests
- [ ] For stack-affecting changes: exercised locally via `make dev` /
      `make live-smoke-test`

```
make verify output (tail)
```

## Science impact

- [ ] **This PR does not change any scientific output** (NDVI values,
      masking, scene selection, statistics, provenance), **or** the
      methodology docs (`docs/scientific-methodology.md`, ADRs) are updated
      and the relevant version constants
      (`PROCESSING_VERSION` / `SCENE_SELECTION_VERSION` / provenance schema
      version) are bumped in this PR.

## Docs

- [ ] Documentation updated where behavior, operations, or interfaces
      changed (`docs/`, `CONTRIBUTING.md`), or no docs are affected.
